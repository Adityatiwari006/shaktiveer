name: shaktiveeer
description: A women safety app with shake alert, SOS button, and live location.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  geolocator: ^12.0.0
  fluttertoast: ^8.2.2
  firebase_core: ^2.27.0
  firebase_auth: ^4.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
